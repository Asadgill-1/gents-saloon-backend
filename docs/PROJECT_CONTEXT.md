# Project Context

Log of decisions and context so any AI picking up this repo has full history. Append new entries at the bottom, don't rewrite old ones.

> **Current state is not inferred from the historical log below.** Read [../START_HERE.md](../START_HERE.md) for the authoritative checkpoint, remaining gates, and exact next step.

## What this is

**UAE multi-tenant barbershop platform** (upgraded from single-shop reception system on 2026-07-16 when the owner delivered the full spec in [../Prompt.md.txt](../Prompt.md.txt)): per-shop Telegram bot suite (Customer/Receptionist/Barber/Owner) + global Master bot, AI receptionist (Moonshot, EN/AR/HI/UR), dynamic queue + appointments, POS with hybrid commissions/advances/ledger, tablet web UI, platform-owner dashboard.

## Stack decisions (locked in)

- **Backend:** Python 3.12+, FastAPI, aiogram 3 (bots), Celery 5 + Beat, Redis
- **Database/Auth/Realtime:** Supabase (RLS mandatory)
- **AI:** Moonshot (OpenAI-compatible), intent-extraction + tools only
- **Frontend:** Next.js App Router + Tailwind + shadcn/ui (corrected from generic "React" after owner spec)
- **Deploy:** dev = local polling; prod = 1 VPS Docker Compose (api/worker/beat/redis/caddy)

## Current status

Phase 0 foundations, native dependency runtime, API/Celery delivery, Supabase baseline, commits/pushes, and all three GitHub Actions pipelines are verified. Phase 0 is still open for owner credential rotation and authenticated repository-protection evidence. Phase 1 T1.0–T1.6 are implemented and locally verified; its dated security audit is recorded but not passed because inherited credential/repository-control gates and a live private-Storage round trip remain open. By explicit owner direction, Phase 2 has started: T2.0–T2.6 are complete and T2.7 reports/e-invoicing boundary is next. Phases 3–6 have not started.

## Historical status snapshots

2026-07-16 — At that historical checkpoint, the full production plan was written and nothing was built. This entry is retained as history; it is not the current state.

## Decision log

- 2026-07-16: Repo scaffolded; explicitly no code yet.
- 2026-07-16: Owner spec delivered (Prompt.md.txt) — multi-tenant bots + POS + AI; supersedes "reception system" scope.
- 2026-07-16: Owner Q&A (4 decisions): **(1)** dev local now → VPS Compose for prod; **(2)** web UI English-only (bots stay 4-language); **(3)** customer phone via Telegram contact-share on first booking, skippable; **(4)** booking = live queue **+ future appointments** (slots → promoted to queue at T-30min).
- 2026-07-16: Full plan written (10 docs). Design tokens locked in DESIGN_SYSTEM.md (dark-first slate + gold brand, Fira Sans/Fira Code/Cormorant). Anti-hallucination: volatile facts (Moonshot model IDs, package versions) marked `VERIFY AT BUILD TIME` in docs.

- 2026-07-16 (later): Owner created 3 GitHub repos (D13 in MASTER_PLAN): `gents-saloon-backend` (this repo — canonical docs + backend + supabase), `saloon-shop-dashboard` (Phase 2 app), `saloon-gents-system-owner-dashboard` (Phase 3 app). Both dashboards deploy on **Vercel from GitHub main**. Consequence: Phase 2 and Phase 3 are now **two separate Next.js apps** (was: one app with /admin) — phase docs updated; shared components get copied between dashboard repos (ponytail: shared package only if a 3rd consumer appears). Local `frontend/` placeholder removed.

<!-- Append new entries below as prompts/features/decisions come in -->

- 2026-07-24: Production SaaS revision started. Commercial tenant changed from `shop` to `business`; one primary owner may own multiple shops, while all operational records remain shop-scoped. Authorization now resolves database ownership/memberships instead of one JWT `shop_id`.
- 2026-07-24: Manual/cash SaaS billing locked. Each business chooses business-wide or per-shop billing, never both at once. `paid_until` is inclusive; expiry is 00:05 the following Asia/Dubai day with no grace period. Suspension blocks all tenant surfaces but preserves platform-admin operations.
- 2026-07-24: Offboarding locked as export-first soft archive. Product does not hard-delete tenants. Targeted customer anonymization remains possible subject to legal/financial retention.
- 2026-07-24: Money contract corrected. Fixed percentage and tier/threshold commission remain; tips are 100% barber. Advances create cash disbursement plus receivable and are deducted exactly once during payout. Payout runs/items and append-only balanced journal are required.
- 2026-07-24: Production persistence uses a server-only async PostgreSQL transaction pool for mutations. Supabase remains Auth/RLS reads/Realtime/admin. Redis is never the source of truth for queue counters, bookings, subscriptions, or money.
- 2026-07-24: Public queue privacy tightened to token/status/estimate only; customer first names were removed. Telegram webhooks now use opaque bot IDs with the secret only in Telegram's secret-token header.
- 2026-07-24: `C:\Users\HPUSER\Downloads\PLAN.md` was supplied as the approved revision brief and merged into the canonical docs. The downloaded copy is reference input, not a canonical project file.
- 2026-07-25: Phase 0 implementation started. Backend now has locked Python dependencies, validated settings, PostgreSQL/Redis clients, Celery with a real health worker task, FastAPI health routes, tests, development Compose, and SHA-pinned CI. Both frontend repositories now have locked Next.js 16/Tailwind/Supabase SSR foundations, safe headers, and test/build CI. Next.js DevTools MCP was evaluated but not installed because its current `0.4.0` release produces unresolved high-severity npm audit findings; activate it only after an audited fixed release. Docker Desktop could not start on the workstation, so live dependency/worker-broker verification remains pending; unit/config/build checks are green.
- 2026-07-25: A full dated security audit is mandatory after every phase. No phase completes with an unresolved Critical/High finding; Medium/Low risks require explicit acceptance, an owner, and a remediation trigger/date. Phase 0 audit is recorded but not passed pending owner token rotation and external Docker/remote-CI evidence.
- 2026-07-25: `START_HERE.md` became the authoritative cross-repository handoff. Every future work session updates it, the active phase, requirements status, and affected repository-local status docs so another agent can resume without reconstructing history.
- 2026-07-25: Historical dashboard labels “Phase 2 app” and “Phase 3 app” are superseded by the current seven-phase plan. The shop dashboard is Phase 4 and the platform-owner dashboard is Phase 5; their Phase 0 technical foundations do not mean those product phases have started.
- 2026-07-25: Docker Desktop cannot run on this workstation because firmware virtualization is disabled and WSL is incomplete. The approved local fallback is PostgreSQL 17.10 plus a separate localhost-only, password-protected Memurai instance on port 6380. Tracked start/stop scripts reproduce the setup without weakening the PowerShell execution policy. Live FastAPI readiness, Redis fail-closed behavior, PostgreSQL continuity, and Celery task delivery passed, so Docker is no longer a Phase 0 local-proof blocker.
- 2026-07-25: The official Supabase MCP was configured globally with OAuth, scoped to project `butoxkmxkaybajoqrpza`, with database/debugging/development/docs/functions/branching features. Official Supabase agent skills were installed globally. The first read-only baseline found no application tables, project migrations, Edge Functions, or security/performance advisor findings. Full MCP write access is owner-approved only for this empty development project; use read-only mode or a development branch before production data exists.
- 2026-07-25: At that historical checkpoint, a second-agent Phase 0 review confirmed the local security controls and identified two handoff risks: the then-current implementation was uncommitted/unpushed, and Phase 1–6 files were intentionally concise rather than executable task plans. The Git/CI risk was resolved on 2026-07-26 below. Expand only the active next phase before coding it; do not inflate all future phase files prematurely.
- 2026-07-25: Before Phase 2 money implementation, the owner must make an explicit scope decision on double-entry journal, payout runs, credit notes, cash shifts, VAT/legal profiles, and offboarding workflows. Current canonical contracts retain them for integrity, but implementation timing remains a product decision.
- 2026-07-25: A new npm advisory affects `brace-expansion` through both dashboards' lint-only `eslint-config-next` chain. Production dependency audits remain clean and all checks/builds pass. The forced npm remediation is API-incompatible and was reverted. Track P0-SEC-010 for a compatible upstream release or explicit temporary CI risk decision.
- 2026-07-25: P0-SEC-010 was resolved without a risk exception. Both dashboards now use Next's documented direct `@next/eslint-plugin-next` flat config with ESLint 10, TypeScript ESLint, and React Hooks rules instead of the vulnerable convenience config. A normal clean `npm ci`, full audit, lint, types, tests, and production build pass in each repository.
- 2026-07-25: Read-only remote verification found the three GitHub repositories still at their original three commits; local Phase 0 work and workflows have not been pushed or executed remotely. GitHub CLI is not installed, so settings that require authenticated repository administration remain owner/remote work.
- 2026-07-26: The earlier uncommitted/unpushed handoff risk is resolved. Backend/canonical work was pushed through `c511eca`, shop dashboard through `9b25b12`, and platform dashboard through `b22ee5b`; their matching GitHub Actions runs all passed. Authenticated evidence for branch protection, required-check enforcement, GitHub secret scanning, and push protection remains open.
- 2026-07-25: The owner directed work to Phase 1 while Phase 0 external gates remain visible. T1.0–T1.1 created and remotely applied three forward migrations for tenant/RLS core, ordered FK indexes, and explicit deny policies on backend-only tables. Local native PostgreSQL reconstruction and actor isolation tests pass. Remote Supabase Security Advisor has zero findings; Performance Advisor has only expected unused-index INFO notices on empty tables. No remote test/customer rows were inserted.
- 2026-07-25: Phase 1 T1.2 completed. The FastAPI backend verifies Supabase ES256/RS256 access tokens against the project JWKS with issuer, audience, expiry, issued-at, and not-before validation, accepts only the UUID subject as identity, and derives active profile, platform-admin, owner, shop, and role context from PostgreSQL. `GET /api/v1/me/context` returns a generic 401/403/503 on authentication, inactive identity, or authorization-database failure. Unit/cryptographic tests and a real migrated-PostgreSQL integration gate prove owner-two-shop, staff-one-shop, and inactive-user behavior. Windows database tests use Selector event-loop policy because psycopg async rejects Proactor; Linux CI remains unchanged.
- 2026-07-25: Phase 1 T1.3 completed. `POST /api/v1/platform/tenants` rechecks active platform-admin status inside a short PostgreSQL transaction and atomically writes the application owner profile, `business_owners` primary-owner relation, business, first shop, billing-mode-compatible subscription, audit, outbox, and idempotent response. Concurrent same-key calls produce one tenant; changed payload conflicts; forced late unique failure rolls back every row. The existing Supabase Auth UUID is a prerequisite because the external Admin invite API cannot join the database transaction, and direct writes to managed Auth tables are prohibited. An owner is not inserted into `shop_memberships`, whose enum correctly remains manager/receptionist/barber only.
- 2026-07-25: Phase 1 T1.4 completed. Onboarding now includes the initial immutable cash receipt. Platform-admin-only, idempotent services record cash receipts and mirror reversals, suspend/resume subscriptions, and atomically switch billing mode under consistently ordered row locks. Non-payment resume requires current paid coverage; other expired resumes require coverage or a reasoned expiring override. Entitlement uses PostgreSQL state plus an exact 00:05 Asia/Dubai deadline while the database stays UTC. Celery Beat runs the idempotent expiry worker. Local reconstruction, clock/state/concurrency tests, full backend checks, and dependency audit pass. Remote migration `20260725165706_subscription_entitlement_hardening` is applied, application rows remain zero, and Supabase Security Advisor has zero findings.
- 2026-07-25: Subscription receipt reversal corrects immutable cash evidence with a linked positive mirror row and does not silently retract previously granted access. Access removal is a separate explicit, audited suspension/coverage decision. This avoids hidden entitlement changes from accounting corrections.
- 2026-07-26: Phase 1 T1.5 completed. Tenant route IDs are locators only; one shared FastAPI dependency re-derives actor/shop access from PostgreSQL and applies the entitlement state. A platform administrator bypasses only the tenant entitlement gate and retains a separate global shell. Public queue availability exposes only `available`/`unavailable`, and the Telegram gate acknowledges suspended trusted updates without running operations. Both dashboard shells verify Supabase claims and use the session only to forward the raw access token to FastAPI. No Phase 4/5 product UI or Phase 3 webhook transport was started. Local reconstruction, API/bot/public agreement tests, frontend contract/build checks, dependency audits, and live Supabase advisors pass.
- 2026-07-26: Phase 1 T1.6 completed. Export schema `2026-07-26.v1` builds an inspected JSON/CSV ZIP from explicit Phase 1 allowlists, redacts credential-like audit fields, excludes secret/internal delivery data, and records byte size plus SHA-256. Platform-admin-only idempotent APIs request exports, issue short-lived signed URLs after reauthorization, confirm delivery, freeze offboarding scope, and soft-archive only after delivery. A Celery worker claims rows with `FOR UPDATE SKIP LOCKED`, recovers stale processing, stores only safe failure codes, and deletes expired objects while retaining metadata. Migration `20260726061032_export_offboarding_hardening` is applied locally/remotely; the private ZIP-only `tenant-exports` bucket exists; reconstruction and 8 database-gated tests pass; remote tables remain empty and Security Advisor has zero findings.
- 2026-07-26: Phase 1 T1.7 security audit executed and remains open. The audit found missing distributed route throttling as High; it was fixed with Redis atomic counters, hashed actor/IP keys, route-class budgets, `429 Retry-After`, fail-closed `503`, and unit coverage. No new unresolved Critical/High defect remains in Phase 1 code. Commits, pushes, and remote CI later passed in all repositories. The audit still cannot pass until the owner rotates the inherited Telegram credentials, authenticated repository-protection settings are proven, and a live private Storage upload/signed-download/delete round trip is completed with an approved backend runtime credential. Complexity review found no useful deletion or dependency simplification; source debt scan found no `ponytail:` markers.
- 2026-07-26: The owner explicitly approved the full Phase 2 financial-integrity scope: shop legal/VAT profiles, receipt and credit-note counters, cash shifts/movements, immutable transaction/payment/commission snapshots, refunds/credit notes, tips, advances, payout runs, and an append-only balanced double-entry journal. These are no longer optional timing decisions. Provider-neutral UAE e-invoicing readiness stays in scope, but no accredited service provider is selected in code.
- 2026-07-26: Current official UAE sources were rechecked before Phase 2. FTA guidance requires VAT-registered suppliers to issue tax invoices for taxable supplies and rounds invoice lines to the nearest fils. The Ministry of Finance e-invoicing portal remains the authoritative changing source; current rules cover B2B/B2G while B2C is excluded until a later decision, with phased implementation from 2027. The system therefore keeps ordinary saloon B2C receipts separate and builds only a provider-neutral B2B/B2G adapter boundary.
- 2026-07-26: Phase 2 T2.0–T2.1 completed. Two forward migrations add shop-isolated services, customers, business hours, closures, barber schedules/breaks/leave/unavailability, effective legal profiles, and immutable effective commission rules. SQL validates tenant/barber references, E.164 phone shape, VAT/TRN/document combinations, non-overlapping periods, tier JSON, overnight flags, and break-within-shift. RLS allows operational reads by least privilege, denies browser writes, hides customer/legal data from barbers, hides commission rules from receptionists, and lets barbers read only their own override. Local reconstruction/RLS/adversarial tests pass. Remote Supabase has 24 empty RLS-enabled application tables, seven migrations, zero browser mutation policies, zero missing FK indexes, and zero Security Advisor findings.
- 2026-07-26: Phase 2 T2.2 completed. PostgreSQL now owns five-minute appointment holds, the queue/appointment/walk-in state machine, immutable multi-service snapshots, shop-timezone queue counters, deterministic Any Barber allocation, reschedule/cancel/no-show history, and T-30 promotion. One transaction-scoped advisory lock per shop serializes allocation at the approved 30–50-shop scale; GiST exclusion and unique counters remain final database backstops, and Redis is absent from booking correctness. FastAPI mutations reauthorize owner/manager/receptionist/platform actors and entitlement inside the transaction, require idempotency keys, and write audit/outbox with the domain change. Celery Beat scans PostgreSQL each minute for hold expiry/promotion. Local reconstruction, RLS, state, concurrency, worker-replay, and Redis-independence gates pass. Remote Supabase has 27 empty RLS-enabled application tables, nine migrations, zero booking browser mutations, zero missing FK indexes, and zero Security Advisor findings. T2.2 also exposed and fixed the shared entitlement lock’s invalid `FOR SHARE` on a nullable outer-join row.
- 2026-07-26: Official UAE sources were rechecked for T2.3. FTA guidance continues to require line-item tax rounding to the nearest fils and distinguishes full/simplified tax invoices. Ministry of Finance Resolution 66 of 2026 moved the ≥AED 50,000,000 business deadline to appoint an Accredited Service Provider to 30 October 2026 while retaining mandatory implementation from 1 January 2027; smaller businesses remain scheduled to appoint by 31 March 2027 and implement by 1 July 2027. Current e-invoicing scope remains B2B/B2G, so ordinary B2C saloon documents stay separate and no provider is embedded.
- 2026-07-26: Phase 2 T2.3 completed. Two forward migrations add simplified tax-invoice support, fiscal-year shop sale/credit-note counters, cash shifts, and append-only cash movements. PostgreSQL enforces one open case-insensitive register per shop, same-tenant references, source uniqueness, one-way close, and independent expected/count/variance reconciliation. FastAPI implements current legal-profile selection, shift open/preview/manual movement/close, and an internal source-backed movement contract for later cash sale/refund/advance/payout modules. Mutations reauthorize operator and entitlement inside one short transaction, require idempotency, and write audit/outbox atomically. Parallel counters/open attempts, same-key replay, VAT/non-VAT fixtures, card exclusion, cash equations, post-close immutability, RLS/IDOR, reconstruction, and targeted security checks pass. Remote Supabase has 30 empty RLS-enabled application tables, eleven migrations, zero legal/cash browser mutations, and zero Security Advisor findings.
- 2026-07-26: Phase 2 T2.4 completed. Checkout accepts an existing completed booking; counter sales use a walk-in booking so there is one financial path. PostgreSQL supplies customer/barber/service/legal/commission facts, while the client may provide only line discounts, cash/card tender, a separate tip, and the required open cash shift. Decimal half-up calculations support inclusive/exclusive VAT and keep tips separate. One idempotent transaction allocates the receipt and writes immutable transaction/item/payment snapshots, restricted commission snapshots, the cash-only shift movement, balanced journal, audit, and outbox. Deferred checks independently reconcile item/header/payment/commission totals, cash-shift presence, and journal balance; completed financial rows are append-only. Receptionists cannot read commission or journal rows, barbers can read only their own commission and no payment rows, and owners/platform administrators retain approved scope. Local reconstruction, exact/range calculation, RLS/IDOR, golden AED 120 → 25/95, split-tender/tip, concurrency/replay, and security gates pass. Remote migrations `20260726092654_pos_checkout_journal` and `20260726094355_checkout_fk_index_hardening` are applied; all 37 public tables use forced RLS, the only persisted rows are eight controlled journal accounts, browser mutation policies and missing FK indexes are zero, and Security Advisor has zero findings. The second migration and a stricter local index test closed the one INFO-level composite-FK index notice found by the live Performance Advisor.
- 2026-07-26: The T2.4 checkpoint is committed and pushed across all repositories: backend/canonical `f02928b`, shop dashboard `34b6aed`, and platform dashboard `cc447a1`. Their matching GitHub Actions runs completed successfully. The next implementation task is T2.5; authenticated repository-protection evidence remains a separate open owner gate.
- 2026-07-26: The synchronized T2.4 handoff correction is committed and pushed across all repositories: backend/canonical `54d4370`, shop dashboard `eae7426`, and platform dashboard `fa8a582`. Their matching remotes are synchronized; this supersedes the earlier T2.4 checkpoint hashes above without rewriting history.
- 2026-07-26: Phase 2 T2.5 completed locally and on the project-scoped Supabase development database. One correction endpoint supports bounded partial/full refunds and a deliberately conservative full void only for cash-only original tender while its original shift remains open. The service derives net/VAT, commission, tip, tender, cash, and reversing journal values from immutable checkout snapshots; any transaction containing card tender must use a credit-note refund because terminal settlement is not modeled. Four append-only forced-RLS correction tables separate operational items, restricted commission reversals, and payments. Deferred PostgreSQL validation serializes and cumulatively bounds corrections, reconciles the final proportional slice exactly, links the reversal journal to checkout, requires an exact cash movement, and independently blocks card-tender voids. Local reconstruction/RLS/concurrency tests prove same-key replay, one winner for competing final refunds, exact partials, full cash void, card-tender void denial at service/database layers, original immutability, least-privilege reads, and append-only enforcement. Remote migrations `20260726105629_correction_credit_notes` and `20260726111301_correction_void_tender_hardening` are applied: all 41 public tables use forced RLS, only eight controlled journal-account reference rows exist, browser mutation policies and missing FK indexes are zero, and Security Advisor has zero findings. The T2.5 security checkpoint has no unresolved Critical/High finding; the full dated Phase 2 audit remains due at T2.8. T2.6 is next.
- 2026-07-26: The T2.5 checkpoint is committed, pushed, synchronized, and green in GitHub Actions across all repositories: backend/canonical `8ae961c`, shop dashboard `be9b180`, and platform dashboard `8c73502`.
- 2026-07-26: Phase 2 T2.6 completed locally and on the project-scoped Supabase development database. Four forced-RLS tables add advances, payout runs/items, and append-only advance applications. Only active business owners and platform administrators may mutate. Advances disburse exactly once through an open cash shift and balanced receivable journal; no cancellation state exists. Payouts use closed half-open UTC periods, immutable transaction/correction commission and tip snapshots, reasoned signed adjustments, non-overlapping non-cancelled shop periods, and `draft → approved → paid` or pre-payment cancellation. Approval reserves one run per shop and bounds deductions by both outstanding advances and gross payable; payment alone creates applications, cash settlement, and exact grouped journal postings. Concurrent tests prove same-key replay, one payment winner, one deduction, exact cash/journal effects, and cancellation without balance changes. RLS proves receptionist denial, own-barber visibility, owner/business access, unrelated-owner isolation, and platform access. Native reconstruction, all SQL/RLS suites, 13 database integrations, 61 backend tests, type/lint/format/dependency gates, history/current-source secret scans, and the task security checkpoint pass. Remote migration `20260726115339_advance_payout_settlement` is applied: all 45 public tables use forced RLS, only nine controlled journal-account rows exist, finance rows/browser mutation policies/missing FK indexes/exposed private validators are zero, and Security Advisor has zero findings. T2.7 reports and provider-neutral e-invoicing is next; the full dated Phase 2 audit remains due at T2.8.
- 2026-07-26: T2.6 source and handoff changes are local and uncommitted in the three repositories. The last pushed/green checkpoint remains T2.5 (`8ae961c`, `be9b180`, `8c73502`). Do not claim T2.6 remote Git/CI evidence until it is committed, pushed, and the matching workflows pass.
