# Copy to /etc/gents-saloon/compose.env with mode 0600.
# Never commit the populated file.
ACME_EMAIL=ops@example.invalid
API_DOMAIN=api.example.invalid
REDIS_PASSWORD=replace-in-owner-secret-store
RUNTIME_ENV_FILE=/etc/gents-saloon/runtime.env
